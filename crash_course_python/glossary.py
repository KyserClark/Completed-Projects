glossary = {
    'list': 'mutable ordered objects grouped together',
    'tuple': 'immutable ordered objects grouped together',
    'boolean': 'can be labeled as True or False',
    'dictionary': 'unordered key value pairs',
    'string': 'variable defined by programmer in-between quotation marks'
    }

print(f"list - {glossary['list']}")
print(f"tuple - {glossary['tuple']}")
print(f"boolean - {glossary['boolean']}")
print(f"dictionary - {glossary['dictionary']}")
print(f"string - {glossary['string']}")
